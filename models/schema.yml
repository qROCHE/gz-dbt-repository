version: 2

sources:
  - name: raw
    schema: gz_raw_data  # Nom du dataset BigQuery
    description: greenweez raw data source
    tables:
      - name: sales
        identifier: raw_gz_sales  # Correction du mot "identifeir"
        description: sales of greenweez - we have one row per product_id found in each order_id
        columns:
          - name: orders_id
            description: order identifier
          - name: pdt_id
            description: product identifier
          - name: order_product_id
            description: primary key of the table (orders_id + pdt_id concatenated)
            tests:
              - unique
              - not_null

      - name: product
        identifier: raw_gz_product
        description: purchase_price by products_id
        columns:
          - name: products_id
            description: primary key of the table
            tests:
              - unique

      - name: ship
        identifier: raw_gz_ship
        description: shipment_fee by order_id
        columns:
          - name: orders_id
            description: primary key of the table
            tests:
              - unique